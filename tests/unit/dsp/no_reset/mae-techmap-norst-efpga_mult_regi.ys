# yosys -m wildebeest -s mae-techmap-norst-efpga_mult_regi.ys
# Plain $dff (no reset): input registers absorbed, clk connected, resetn tied 1'b1
read_verilog <<EOF
module efpga_mult_regi_norst_map_inst
  # (
     parameter INPUT_WIDTH = 18,
     parameter OUTPUT_WIDTH = 36
     )
   (
    input                                   clk,
    input signed [(INPUT_WIDTH-1):0]        a,
    input signed [(INPUT_WIDTH-1):0]        b,
    output [(OUTPUT_WIDTH-1):0]             y
    );

   reg signed [(INPUT_WIDTH-1):0] a_reg;
   reg signed [(INPUT_WIDTH-1):0] b_reg;

   always @(posedge clk) begin
      a_reg <= a;
      b_reg <= b;
   end

   assign y = a_reg * b_reg;

endmodule
EOF

synth_fpga -partname z1010
select -assert-count 1 */t:efpga_mult_regi
select -assert-count 1 */t:efpga_mult_regi %x:+[clk] */w:clk %i
select -assert-count 0 */t:efpga_mult_regi %x:+[resetn] */w:resetn %i
